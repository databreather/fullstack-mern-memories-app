import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
	appBarSearch: {
		borderRadius: 4,
		display: "flex",
		padding: "16px",
		margin: "20px 0",
	},

	pagination: {
		borderRadius: 4,
		marginTop: "1rem",
		padding: "16px",
	},
}));
